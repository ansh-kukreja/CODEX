const sum = (a,b) => a+b;
const mul = (a,b) => a*b;
const lol = 9;

let obj = {
    sum: sum,
    mul: mul,
    lol: lol
}

// module.exports = "hello";

// module.exports.sayHello = (n) => "Hello "+n;

module.exports = obj;
